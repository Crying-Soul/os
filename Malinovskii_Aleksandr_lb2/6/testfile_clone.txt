Parent (before clone) writes line
Clone child writes line
Parent (after clone) writes line
Parent (before clone) writes line
Clone child writes line
Parent (after clone) writes line
Parent (before clone) writes line
Clone child writes line
Parent (after clone) writes line
Parent (before clone) writes line
Clone child writes line
Parent (after clone) writes line
