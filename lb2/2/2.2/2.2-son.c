/**
 * 2.2-son.c
 * 
 * Функциональность:
 * 1. Исполняемый модуль для демонстрации работы процессов:
 *    - Отображение собственного PID
 *    - Фиксация состояния системы в момент выполнения
 *    - Чистое завершение с возвратом статуса
 * 
 * 2. Диагностические возможности:
 *    - Логирование таблицы процессов
 *    - Четкая идентификация процесса в выводе
 *    - Интеграция с родительским процессом
 * 
 * 3. Особенности выполнения:
 *    - Запускается исключительно через father.c
 *    - Использует стандартные механизмы завершения
 *    - Сохраняет системную информацию перед выходом
 */
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {
  printf("Son process is running with PID: %d\n", getpid());

  // Фиксируем состояние таблицы процессов
  system("ps -H f | tee son_processes.log");

  printf("Son process finished.\n");

  return 0;
}
